<%= form_with(model: pr_sweetness) do |form| %>
  <% if pr_sweetness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr_sweetness.errors.count, "error") %> prohibited this pr_sweetness from being saved:</h2>

      <ul>
        <% pr_sweetness.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sweetness %>
    <%= form.text_field :sweetness %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
