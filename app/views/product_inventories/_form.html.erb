<%= form_with(model: product_inventory) do |form| %>
  <% if product_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_inventory.errors.count, "error") %> prohibited this product_inventory from being saved:</h2>

      <ul>
        <% product_inventory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
