<%= form_with(model: shopping_session) do |form| %>
  <% if shopping_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shopping_session.errors.count, "error") %> prohibited this shopping_session from being saved:</h2>

      <ul>
        <% shopping_session.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
