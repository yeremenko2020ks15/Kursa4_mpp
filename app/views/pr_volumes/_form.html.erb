<%= form_with(model: pr_volume) do |form| %>
  <% if pr_volume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr_volume.errors.count, "error") %> prohibited this pr_volume from being saved:</h2>

      <ul>
        <% pr_volume.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :volume %>
    <%= form.text_field :volume %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
