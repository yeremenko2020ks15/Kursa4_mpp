<%= form_with(model: pr_country) do |form| %>
  <% if pr_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr_country.errors.count, "error") %> prohibited this pr_country from being saved:</h2>

      <ul>
        <% pr_country.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
