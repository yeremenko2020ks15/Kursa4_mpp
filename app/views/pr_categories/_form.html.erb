<%= form_with(model: pr_category) do |form| %>
  <% if pr_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr_category.errors.count, "error") %> prohibited this pr_category from being saved:</h2>

      <ul>
        <% pr_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
