<%= form_with(model: pr_brand) do |form| %>
  <% if pr_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr_brand.errors.count, "error") %> prohibited this pr_brand from being saved:</h2>

      <ul>
        <% pr_brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
